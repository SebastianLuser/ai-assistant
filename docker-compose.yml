services:
  asistente:
    build:
      context: .
    restart: always
    ports:
      - "8081:8080"
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - sqlite_data:/app/data
      - ./skills:/app/skills:ro
volumes:
  sqlite_data:
